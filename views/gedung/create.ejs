<!doctype html>
<html lang="id">
<head>
  <%- include('../partials/_head', { title: title }) %>
</head>
<body>

<%- include('../partials/_sidebar', { user: user }) %>

<main id="content">
  <header class="bg-background sticky inset-x-0 top-0 isolate flex shrink-0 items-center gap-2 border-b z-10">
    <div class="flex h-14 w-full items-center gap-2 px-4">
      <button type="button" onclick="document.dispatchEvent(new CustomEvent('basecoat:sidebar'))"
        aria-label="Toggle sidebar" class="btn-sm-icon-ghost mr-2 size-7 -ml-1.5">
        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect width="18" height="18" x="3" y="3" rx="2"/><path d="M9 3v18"/></svg>
      </button>
      <nav aria-label="breadcrumb" class="flex items-center gap-1.5 text-sm">
        <a href="/gedung" class="text-muted-foreground hover:text-foreground transition-colors">Kelola Gedung</a>
        <span class="text-muted-foreground">/</span>
        <span class="text-foreground font-medium">Tambah Gedung</span>
      </nav>
      <div class="ml-auto flex items-center gap-2">
        <button type="button" onclick="document.dispatchEvent(new CustomEvent('basecoat:theme'))"
          aria-label="Toggle dark mode" class="btn-icon-outline size-8">
          <span class="hidden dark:block"><svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="4"/><path d="M12 2v2M12 20v2M4.93 4.93l1.41 1.41M17.66 17.66l1.41 1.41M2 12h2M20 12h2M6.34 17.66l-1.41 1.41M19.07 4.93l-1.41 1.41"/></svg></span>
          <span class="block dark:hidden"><svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/></svg></span>
        </button>
        <a href="/logout" class="btn-outline btn-sm gap-1.5 hidden sm:flex">
          <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"/><polyline points="16 17 21 12 16 7"/><line x1="21" y1="12" x2="9" y2="12"/></svg>
          Logout
        </a>
      </div>
    </div>
  </header>

  <div class="p-4 sm:p-6 max-w-3xl mx-auto space-y-6">
    <div class="mb-6">
      <h1 class="text-2xl font-bold tracking-tight">Tambah Gedung</h1>
      <p class="text-muted-foreground text-sm mt-1">Isi form berikut untuk menambahkan gedung baru.</p>
    </div>

    <% if (flash) { %>
    <div class="alert <%= flash.type === 'success' ? 'alert-success' : 'alert-destructive' %> mb-4">
      <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="12"/><line x1="12" y1="16" x2="12.01" y2="16"/></svg>
      <div><p class="text-sm"><%= flash.message %></p></div>
    </div>
    <% } %>

    <%# Tampilkan error jika ada %>
    <% if (errors && errors.length > 0) { %>
    <div class="alert alert-destructive mb-6">
      <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="12"/><line x1="12" y1="16" x2="12.01" y2="16"/></svg>
      <div>
        <p class="font-medium">Terdapat kesalahan:</p>
        <ul class="text-sm list-disc list-inside mt-1">
          <% errors.forEach(e => { %><li><%= e %></li><% }) %>
        </ul>
      </div>
    </div>
    <% } %>

    <div class="card">
      <div class="card-content pt-6">
        <form id="form-tambah-gedung" action="/gedung/store" method="POST" class="space-y-5">

          <div class="field">
            <label class="label" for="name">
              Nama Gedung <span class="text-destructive">*</span>
            </label>
            <input type="text" id="name" name="name"
              value="<%= old.name || '' %>"
              placeholder="Contoh: Gedung A Teknik"
              class="input w-full" required />
            <p class="field-description">Nama lengkap gedung sesuai dokumen resmi.</p>
          </div>

          <div class="field">
            <label class="label" for="code">
              Kode Gedung <span class="text-destructive">*</span>
            </label>
            <input type="text" id="code" name="code"
              value="<%= old.code || '' %>"
              placeholder="Contoh: GDG-A"
              class="input w-full" required />
            <p class="field-description">Kode singkat unik untuk identifikasi gedung.</p>
          </div>

          <div class="field">
            <label class="label" for="description">Deskripsi</label>
            <textarea id="description" name="description" rows="3"
              placeholder="Deskripsi singkat tentang gedung (opsional)..."
              class="textarea w-full"><%= old.description || '' %></textarea>
          </div>

          <div class="flex items-center justify-end gap-3 pt-2">
            <a href="/gedung" class="btn-outline">Batal</a>
            <button type="submit" id="btn-simpan-gedung" class="btn gap-1.5">
              <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M19 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11l5 5v11a2 2 0 0 1-2 2z"/><polyline points="17 21 17 13 7 13 7 21"/><polyline points="7 3 7 8 15 8"/></svg>
              Simpan Gedung
            </button>
          </div>

        </form>
      </div>
    </div>
  </div>
</main>

</body>
</html>
